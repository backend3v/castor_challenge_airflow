;; Reference notes for PgBouncer (optional custom mount).
;; Production config for this repo is driven by the ``edoburu/pgbouncer`` image
;; environment variables in ``docker-compose.yaml``:
;;   POOL_MODE=transaction, MAX_CLIENT_CONN, DEFAULT_POOL_SIZE, AUTH_TYPE, DB_HOST, etc.
;;
;; To override with a file, mount into the container and align with the image docs:
;;   https://hub.docker.com/r/edoburu/pgbouncer/
